<%= form_for @role do |f| %>
<fieldset id="formulario_base">
<legend id="formulario_title"><p>Rol</p></legend>
<div class="form-group">
  <% if @role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@role.errors.count, "error") %> prohibited this role from being saved:</h2>
      <ul>
      <% @role.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
</div>

  <div class="form-group">
    <%= f.label :nombre, for: 'nombre', class: 'col-xs-3 col-sm-2 control-label'  %>
    <div class="col-xs-9 col-sm-10">
    <%= f.text_field :nombre, class: 'form-control', id:'nombre' %>
    </div>
  </div>
  <div class="form-group hidden-xs hidden-sm hidden-md hidden-lg">
    <%= f.label :estatus %>
    <%= f.text_field :estatus, :value =>'A' %>
  </div>
  
  <div class="form-group">
    <div class="col-xs-12">
      <br>
      <%= f.submit nil , class: 'btn btn-default' %>
    </div>
  </div>
  </fieldset>
<% end %>
